import {DndEventsDirective} from './dnd-events.directive';

describe('DndEventsDirective', () => {
    it('should create an instance', () => {
        const directive = new DndEventsDirective();
        expect(directive).toBeTruthy();
    });
});
